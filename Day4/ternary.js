let isRaining=false
// isRaining
// ? console.log('You need a rain coat')
// : console.log('No need for a rain coat')

//condition ? exprIfTrue : expIfFlase

const age=18 
age >18 ? 'beer' : 'jucie'

let name='S' , country='India'
let nameLength=name.length
nameLength >1 ? console.log('It is name') : console.log('It is not a name')